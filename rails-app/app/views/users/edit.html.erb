<a href="<%= user_path(@user) %>" class="button tiny">Back</a>
<h2>Edit yourself</h2>

<%= form_for @user, :url => {:action => "update"} do |f| %>
	<label>Name</label>
	<%= f.text_field :name %>

	<%= f.submit "Save that crap", :class => "button" %>
<% end %>
